
<%= form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%= f.label :title %> 
<br>
<%= f.text_field :title %> 
<br>

<%= f.label :summary %> 
<br>
<%= f.text_area :summary %> 
<br>

<%= f.label :description %>
<br>
<%= f.text_area :description %>
<br>

<%= f.label :contents %>
<br>
<%= f.text_area :contents %>
<br>

<%= f.label :instructions %> 
<br>
<%= f.text_area :instructions %> 
<br>

<%= f.label :price %>
<br>
<%= f.text_field :price %>
<br>

<br>
 <%= f.submit class: 'btn btn-primary' %>

<%= f.label :picture %>
<br>
<%= f.file_field :picture %>
<br>

 <%= f.submit class: 'btn btn-default' %>

<br>

<div class="container">
    <div class="row">
        <div class="col-md-6">
            <%= f.collection_check_boxes(:standard_ids, Standard.all, :id, :path) do |b| %>
                <%= b.label {b.check_box + b.text} %><br>
            <% end %>
        </div>
        <div class="col-md-6">
            <%= f.collection_check_boxes(:perf_exp_ids, PerfExp.all, :id, :code) do |p| %>
                <%= p.label {p.check_box + p.text} %><br>
            <% end %>
        </div>
    </div
</div>
<% end %>


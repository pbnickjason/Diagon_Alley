

<p id="notice"><%= notice %></p>

<%= link_to 'Edit', edit_project_path(@project), style: "color: snow" %> |
<%= link_to 'Back', projects_path, style: "color: snow" %>

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <link href="vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <section class="Jumbotron" style="background-image: url('/img/portfolio/thumbnails/Saturn.jpg');margin-bottom:30px;">
        <div class="container" style="background:#1a1a1a; padding: 20px;">
                <div class="col-sm-6 text-center">
					<h1><%= @project.title %></h1>
                    <img src = <%= @project.image %> style="width: 100%;max-width: 400px;margin:20px;">
                </div>
                <div class="col-sm-6">
                    <div class="row text-center" style= "padding-top: 20px">
                        <h3><%= @project.summary %></h3>
                    </div>
					<div class = "col-sm-6" style="padding-top:80px">
              <%= render 'standards' %>
					</div>
					<div class = "col-sm-6 text-right" style="padding-top:20px">
					 <h3> Materials (maybe order just some?)</h3>
						 <ul style="list-style-position: inside;">
						 <li>Thing 1 ($5)</li>
						 <li> Thing 2 ($999,995.00)</li<
						 </ul>
						 <!--<h3>Price:</h3>
                    <h4><%= @project.price %></h4>
					<% if user_signed_in? %>
                    <% if current_user.projects.exists? @project %>
                    <%= button_to 'Add to Cart', added_path(@project), :method => :get, :class => 'btn btn-primary', :disabled => true %>
                    <% else %>
                    <%= button_to 'Add to Cart', added_path(@project), :method => :get, :class => 'btn btn-primary' %>
                    <% end %>
                    <% end %>-->
					</div>
                </div>
            </div>
    </section>
    <section class="container" style="background:#1a1a1a; padding: 50px;">
            <figure class="col-sm-3" style="outline: 1px solid black;">
                <div class="text-center">
                    <h3>Price:</h3>
                    <h4><%= @project.price %></h4>
					<% if user_signed_in? %>
                    <% if current_user.projects.exists? @project %>
                    <%= button_to 'Add to Cart', added_path(@project), :method => :get, :class => 'btn btn-primary', :disabled => true %>
                    <% else %>
                    <%= button_to 'Add to Cart', added_path(@project), :method => :get, :class => 'btn btn-primary' %>
                    <% end %>
                    <% end %>
                </div>
            </figure>
            <figure class="col-sm-6">
                <p><%= @project.description %></p>
                <h3>Contents</h3>
                <p><%= @project.contents %></p>
                <h3>Instructions</h3>
                <p><%= @project.instructions %></p>
            </figure>
            <figure class="col-sm-3">
                <%= link_to "Workspace", workspace_path(@project), style: 'color: snow' %>
            </figure>
        </div>
    </section>
	</div>
</body>